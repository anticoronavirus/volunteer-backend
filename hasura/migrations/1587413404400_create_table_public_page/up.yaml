- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."page"("uid" uuid NOT NULL, "name" varchar NOT NULL,
      "content" text NOT NULL, "version" integer NOT NULL DEFAULT 1, PRIMARY KEY ("uid")
      , UNIQUE ("uid"), UNIQUE ("version"));
  type: run_sql
- args:
    name: page
    schema: public
  type: add_existing_table_or_view
