- args:
    cascade: false
    read_only: false
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."period_demand"("uid" uuid NOT NULL DEFAULT gen_random_uuid(),
      "period_id" uuid NOT NULL, "profession_id" uuid NOT NULL, "demand" integer NOT
      NULL, PRIMARY KEY ("uid") , FOREIGN KEY ("period_id") REFERENCES "public"."period"("uid")
      ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("profession_id") REFERENCES
      "public"."profession"("uid") ON UPDATE cascade ON DELETE cascade, UNIQUE ("uid"));
  type: run_sql
- args:
    name: period_demand
    schema: public
  type: add_existing_table_or_view
